<!-- Измени:
1. Длина ребер и вершин должна отдельно указываться но с ограничением так же в 15 (сделай так чтобы можно было вручную так же указать а не только стрелочками но так же с ограничением строгим чтоб нельзя было написать например: 16);
Матрица может быть неквадратной!
2. В матрицах помимо значений -1 0 1 можно вводить 2 (это когда ребро начинается и заканчивается в одной и той же вершине (Является петлёй));

В ответе: дай изменнёные готовые результыты по форме и укажи в какой папке это изменять -->


<!-- Измени:
1. В псевдографе нет обработки петель (двоек)
2. В Мультиграфе стоит также добавить обработку двоек, если находится петля, то пишем - "нет это псевдограф"
3. Счетчик петель не работает должен сказать количество двоек в таблице. -->
<!-- 4. Так же измени так чтобы расчёт от кнопки происходил не один раз при загрузке страницы а всегда в каких то модулях это не происходит () -->

<!-- Измени: 
1. Псевдограф некорректно обрабатывает кратные ребра, должен засчитывать только те что объединяют одинаковые вершины
2. Реализуй защиту от дурака например в проверке петель. Например если в строке есть одна двойка то ввод во все остальные клетки строки блокируется пока двойка не будет убрана, подобно и с обычными ребрами если строка содержит 1 и -1 то ввод во все остальные клетки блокируется пока не 1 или -1 не будет убрано.
3. В мультиграфе проблема: программа не различает мультиграф (когда в графе есть кратные ребра) -->

<!-- Измени:
1. Псевдограф по прежнему неправильно определяет  -->

Измени:
1. Я ошибся мне нужны инструменты не для орграфов а для простых графов
2. Нужно убрать -1 и 2 из матриц т.к они относятся к орграфам
3. Нужно немного подругому стилизовать интерфейс не нужно делать белый шрифт на чёрном фоне сделай везде черный шрифт а вместо черного используй очень светлый 
4. Переделай текст инструкции без вяких '/' Чтоб она была как для старого человека 
5. Измени логику вычислений соотвестуя теперь графам по аналогии (сделай всё по форме)