Измени:
1. Длина ребер и вершин должна отдельно указываться но с ограничением так же в 15 (сделай так чтобы можно было вручную так же указать а не только стрелочками но так же с ограничением строгим чтоб нельзя было написать например: 16);
Матрица может быть неквадратной!
2. В матрицах помимо значений -1 0 1 можно вводить 2 (это когда ребро начинается и заканчивается в одной и той же вершине (Является петлёй));

В ответе: дай изменнёные готовые результыты по форме и укажи в какой папке это изменять
